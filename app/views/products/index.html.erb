<h1><%= t "Anime Firuge" %></h1>

<%= link_to "Thêm sản phẩm mới", new_product_path if authenticated? %>
<%= link_to "Đăng nhập", new_session_path unless authenticated? %>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
